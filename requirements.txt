# 渲染与界面
pillow>=10.0          # 模板层 / 字幕层预渲
fastapi>=0.110        # 编辑器后端
uvicorn[standard]>=0.27
python-multipart>=0.0.9   # 上传素材

# 桌面版（可选）。不装也能用：python app.py 会退回系统浏览器。
pywebview>=5.0

# AI 辅助（可选）。不装也能用，AI 面板会自动置灰。
anthropic>=1.0

# 打包成 .app 才需要（可选）
# pyinstaller>=6.0
